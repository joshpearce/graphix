# Enable mouse support (scrolling, pane selection, resizing)
set -g mouse on

# Increase scrollback buffer
set -g history-limit 50000

# Start window numbering at 1
set -g base-index 1
setw -g pane-base-index 1

# Renumber windows when one is closed
set -g renumber-windows on

# Faster key repetition
set -s escape-time 0

# Better colors and UTF-8 support
# Force environment for tmux server (fixes docker exec inheriting TERM=dumb and missing locale)
set-environment -g TERM xterm-256color
set-environment -g LANG C.UTF-8
set-environment -g LC_ALL C.UTF-8
set-environment -g COLORTERM truecolor
set -g default-terminal "screen-256color"
set -ga terminal-overrides ",xterm-256color:Tc"

# Status bar styling
set -g status-style bg=default,fg=white
set -g status-left "[#S] "
set -g status-right "%H:%M"

# Active window styling
setw -g window-status-current-style fg=cyan,bold

# Pane border styling
set -g pane-border-style fg=brightblack
set -g pane-active-border-style fg=cyan

# Easy config reload
bind r source-file ~/.tmux.conf \; display "Config reloaded!"

# Split panes with | and -
bind | split-window -h -c "#{pane_current_path}"
bind - split-window -v -c "#{pane_current_path}"

# Vim-style pane navigation
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R
